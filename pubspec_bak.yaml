name: bill_scanner_app
description: Bill Scanner + Expense Tracker using OCR and Gemini AI
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_mlkit_text_recognition: ^0.11.0
  google_generative_ai: ^0.4.0
  image_picker: ^1.0.7
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_riverpod: ^2.5.1
  pdf: ^3.10.8
  printing: ^5.12.0
  fl_chart: ^0.68.0
  intl: ^0.19.0
  path_provider: ^2.1.2
  flutter_local_notifications: ^17.0.0
  uuid: ^4.3.3
  permission_handler: ^11.3.0
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.8
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
```

---

**2. `.env`** (in project root)
```
GEMINI_API_KEY=AIzaSyCL8U-4L_BWFvgJCWVYuMyBxyE3vJcNaxY